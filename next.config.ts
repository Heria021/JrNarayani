/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['cdn.pixabay.com', 'pixabay.com', 'firebasestorage.googleapis.com', 'res.cloudinary.com'],
  },
};

export default nextConfig;